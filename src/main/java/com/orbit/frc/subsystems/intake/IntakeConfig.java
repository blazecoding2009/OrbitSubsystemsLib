package com.orbit.frc.subsystems.intake;

import com.orbit.frc.subsystems.SubsystemConfig;

/* Configuration for an Intake subsystem */
public class IntakeConfig extends SubsystemConfig {
    /* The radius of the intake wheels, in meters 
     * Can be unset if not using linear speed control
     */
    public double wheelRadius;
}
